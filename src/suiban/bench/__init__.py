"""Benchmarks that run on YOUR hardware and say so.

`suiban bench kv` (bench/kv.py) compares KV V-cache quantization configs on the
locally installed binary and weights. Nothing here ships numbers — reports are
generated locally and honestly labeled.
"""
