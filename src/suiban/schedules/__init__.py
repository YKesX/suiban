"""Scheduled runs (docs/api.md §10, additive 2026-07-21b): cadence math + SQLite
store (store.py), the asyncio scheduler + run execution (runner.py), and the
/v1/schedules HTTP surface (router.py)."""
